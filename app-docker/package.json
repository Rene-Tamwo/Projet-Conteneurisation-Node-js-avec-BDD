{
  "name": "portfolio-node-app",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "lint": "eslint . --ext .js",
    "test:unit": "jest --testPathPattern=tests/unit",
    "test:int": "jest --testPathPattern=tests/integration --runInBand",
    "test": "npm run test:unit && npm run test:int",
    "build": "echo \"Build step (placeholder)\""
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "eslint": "^8.44.0",
    "jest": "^29.6.1",
    "supertest": "^6.3.3"
  }
}

